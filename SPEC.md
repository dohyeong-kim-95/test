# 70bit 이진 최적화 — 채점기 (Surrogate Scorer)

70비트 이진 최적화를 진행하기 전에, 대리모델(surrogate)이 얼마나 믿을 만한지 먼저 재기 위한 채점기.

- **입력 X**: 70비트 binary array
- **출력 Y**: 스칼라 6개

---

## 1. 요구사항

| # | 항목 | 내용 |
|---|------|------|
| 1 | GT 계산식 | 아주 복잡한 결정적(deterministic) 함수, 미분불가능점 다수 |
| 2 | 샘플링 | 7000개 랜덤샘플링하여 (x, y) 기록 |
| 3 | 계산기 | 7000개 데이터 기반 x→y 예측기 |
| 4 | 채점 | 계산기 정확도 산출 로직 |

### 코드 컨벤션
- 파일 하나에 다 모아서 작성
- 한 구역에는 한 기능
- Class는 1개 이하
- List comprehension 사용 가능 / **중첩** list comprehension 사용 불가능
- 모든 코드는 MVP 원칙

### 시도할 보간 방식
1. KNN 거리역수가중보간 (`idw`)
2. Hamming KNN with exponential kernel (`expknn`)
3. Bit intersection model (`bitint`)

### 허용 사항
7000점으로 신뢰도를 얻기 힘든 공간은 `untrusted` / `data lack` 으로 **predict 거절 가능**

---

## 2. 설계

### GT 함수 (`ground_truth`)
출력 6개가 각각 다른 난이도/구조를 갖도록 설계. 전부 `GT_SEED`로 고정되어 재현 가능.

| 출력 | 구조 | 미분불가능 요인 | 난이도 |
|------|------|-----------------|--------|
| y0 | 가중합의 abs 꺾임 3개 | `abs` | 낮음 (저차) |
| y1 | 랜덤 60쌍 pairwise AND → clip | `clip` | 낮음 (2차) |
| y2 | 10비트 블록 7개의 **패리티** 가중합 | 패리티 | 매우 높음 (10차) |
| y3 | 7-윈도우 popcount의 max·min | `max`/`min` | 중간 |
| y4 | 7비트 그룹 10개 점수의 max−min | `max`/`min` | 낮음~중간 |
| y5 | 가중합의 **톱니파** `\|mod(t,9)−4.5\|` | `mod`, `abs` | 매우 높음 |

### 예측기 (`Surrogate`, 클래스 1개)
- `idw` — 해밍거리 k=48 이웃, 가중치 `1/(d+0.5)`
- `expknn` — 같은 이웃, 가중치 `exp(-(d-d_min)/tau)` (언더플로 방지로 d_min 차감)
- `bitint` — `[1, bits(70), pairwise AND(2415)]` 특징에 ridge 회귀

### 신뢰도 / 거절 (`trust`)
세 방식 **공통**으로 같은 신뢰도 점수를 써서 비교가 공정하도록 함.

```
trust = 1 − (이웃 y의 정규화 표준편차) − (최근접 해밍거리 / 70)
```

### 채점 로직 (`risk_coverage`)
단일 임계값 대신 **risk-coverage 곡선**. 신뢰도 상위 X%만 채점했을 때 오차가 실제로 줄어드는지를 봄 —
줄지 않으면 그 신뢰도 점수는 쓸모없다는 뜻.

- 지표: `nMAE = MAE / std`, `R² = 1 − MSE/var` (테스트셋 전체 분산 기준으로 고정 → 부분집합끼리 비교 가능)
- coverage 레벨: 100% / 80% / 50% / 20% / 5%
- **베이스라인(train mean) 필수** — 안 그러면 "평균만 찍기"보다 나쁜지 알 수 없음

---

## 3. 실행 결과

```
train (7000, 70)  test (2000, 70)
popcount train: mean 35.0  min 21  max 50

baseline (train mean)   nMAE 0.812   R2 -0.000
```

| method | y0 | y1 | y2 | y3 | y4 | y5 | 평균 R² |
|--------|-----|-----|-----|-----|-----|-----|---------|
| idw    | +0.441 | +0.421 | −0.015 | +0.118 | +0.457 | −0.027 | +0.232 |
| expknn | +0.462 | +0.436 | −0.027 | +0.122 | +0.471 | −0.036 | +0.238 |
| bitint | **+0.971** | **+0.989** | −0.400 | **+0.443** | **+0.933** | −0.400 | **+0.423** |

### 해석

**① KNN 계열은 이 공간에서 거의 작동하지 않음.**
2^70 공간에 7000점이면 최근접 이웃 해밍거리가 ~25다. 거리 25짜리 이웃은 사실상 무작위 점이라
"국소 보간"이라는 전제 자체가 성립하지 않는다. R² 0.23은 이웃이 아니라 저차 성분이 우연히 새어나온 것.

**② bit intersection(2차 ridge)이 압도적.**
y0/y1/y4에서 R² 0.93~0.99. 7000점으로 2486개 계수를 정하는 건 충분히 가능하고,
70비트 공간에서는 국소 이웃보다 **전역 저차 구조**가 훨씬 값싸게 얻어진다.

**③ y2(패리티)·y5(톱니)는 세 방식 모두 실패.** 여기가 진짜 문제:
- KNN은 R² ≈ 0 (베이스라인 수준) — 모른다는 걸 알고 가만히 있음
- bitint는 R² = **−0.400** — 모르면서 자신 있게 틀림. 평균만 찍는 것보다 나쁨
- 10차 패리티는 2차 특징으로 표현 불가. 데이터를 더 줘도 이 모델 클래스로는 안 됨

**④ 신뢰도 점수가 작동하지 않음.**
coverage를 100%→5%로 줄여도 nMAE가 0.708→0.690으로 거의 안 떨어진다.
현재 `trust`는 신뢰할 수 있는 점과 없는 점을 구분하지 못한다 — 거절 기능이 사실상 죽어 있는 상태.

---

## 4. 다음 단계

1. **거절 로직 재설계** — 현재 `trust`는 무의미. 이웃 기반이 아니라 모델 기반(예: ridge의 leverage,
   또는 부트스트랩/앙상블 분산)으로 바꿔야 함. 세 방식 공통 점수라는 설계는 유지할 가치가 있음.
2. **출력별 거절** — y2/y5는 전체를 거절하는 게 맞다. 특히 bitint의 R²=−0.4는
   최적화기를 적극적으로 잘못된 방향으로 끌고 간다. 출력 단위 `data lack` 판정이 필요.
3. **샘플링 분포** — 현재 uniform이라 popcount가 35±4에 몰려 있다(min 21, max 50).
   최적화기가 popcount 10이나 60 근처를 탐색하면 전부 외삽이다. popcount를 섞어서 뽑아야 함.
4. **차수 확장 검토** — 3차 항까지 가면 y3가 개선될 수 있으나 특징 수가 5.5만 개로 폭증.
   7000점으로는 무리이므로, 쓴다면 특징 선택이 필요.

---

## 5. 실행

```bash
pip install numpy
python3 scorer.py
```
