# 채점기 — 구현과 측정 결과

> 목표와 요구사항은 [GOAL.md](GOAL.md)가 원본이다. 용어는 [GLOSSARY.md](GLOSSARY.md)에 있다.
> 이 문서는 구현과 측정 결과만 담는다.

## 1. 설계

### GT 함수 (`ground_truth`)
출력 6개가 각각 다른 난이도/구조를 갖도록 설계. 전부 `GT_SEED`로 고정되어 재현 가능.

| 출력 | 구조 | 미분불가능 요인 | 차수 |
|------|------|-----------------|-----------|
| y0 | 가중합의 abs 꺾임 3개 | `abs` | 낮음 |
| y1 | 랜덤 60쌍 pairwise AND → clip | `clip` | 2차 |
| y2 | 10비트 블록 7개의 **패리티** 가중합 | 패리티 | **10차** |
| y3 | 7-윈도우 popcount의 max·min | `max`/`min` | 중간 |
| y4 | 7비트 그룹 10개 점수의 max−min | `max`/`min` | 낮음~중간 |
| y5 | 가중합의 **톱니파** `\|mod(t,9)−4.5\|` | `mod`, `abs` | **매우 높음** |

### 예측기 (`Surrogate`, 클래스 1개)
| 이름 | 방식 |
|---|---|
| `idw` | 해밍거리 k=48 이웃의 가중평균, 가중치 `1/(d+0.5)` |
| `expknn` | 같은 이웃, 가중치 `exp(-(d-d_min)/tau)` |
| `bitint` | `[1, bits(70), pairwise AND(2415)]` 특징에 ridge 회귀 |
| `kinterp` | `exp(-d/20)` 커널, **nugget=0** — `K·a = y`를 풀어 학습점을 정확히 통과 |

### 시뮬레이터 3층 (`build_lookup` → `simulate`)
```
1층  룩업 테이블    아는 x → 기록된 y 그대로
2층  모델          나머지 전부
3층  거절          출력별 신뢰도 + popcount 범위 밖
```
아는 점은 신뢰도와 무관하게 무조건 통과시킨다 (측정값이므로).

### 신뢰도 / 거절
```
학습 데이터의 80%를 5번 다르게 뽑아 5개 모델을 적합
trust = −(5개 예측의 표준편차) / (출력 표준편차)      # 출력별로 따로
```
임계값은 테스트셋을 calib 1000 / eval 1000으로 나눠 calib에서만 정하고, eval에서 보고한다.
**최소 coverage 5% 하한**이 있다 — 소수 표본에서 우연히 R²가 높게 나온 지점을 임계값으로
잡는 것을 막는다 (이것 없이는 kinterp의 y2가 2%/R²−0.62로 새어 나갔다).

---

## 2. 채점 2축

### 축 1 — 데이터가 있는 x에서 정확히 그 y가 나오는가 (학습점 최대오차)

| method | y0 | y1 | y2 | y3 | y4 | y5 |
|---|---|---|---|---|---|---|
| idw | 4.3 | 4.7 | 4.9 | 1.5 | 1.9 | 1.3 |
| expknn | 0.19 | 0.23 | 0.25 | 0.07 | 0.09 | 0.07 |
| bitint | 1.4 | 1.8 | 9.1 | 1.7 | 0.86 | 3.0 |
| **kinterp** | **7.7e-14** | **1.5e-13** | **8.2e-13** | **1.5e-13** | **5.7e-14** | **1.8e-13** |
| 룩업 적용 후 (전부) | 0 | 0 | 0 | 0 | 0 | 0 |

`kinterp`만 룩업 없이도 통과한다. `idw`가 최악인 이유는 k=48 평균이라 거리 0인 자기 자신도
전체 가중치의 절반 정도밖에 못 받기 때문이다.

### 축 2 — 데이터가 없는 곳에서의 보간 품질 (test R², 베이스라인 +0.003)

| method | y0 | y1 | y2 | y3 | y4 | y5 |
|--------|-----|-----|-----|-----|-----|-----|
| idw    | +0.465 | +0.393 | +0.022 | +0.101 | +0.450 | −0.026 |
| expknn | +0.483 | +0.413 | +0.012 | +0.098 | +0.470 | −0.027 |
| bitint | **+0.971** | **+0.984** | −0.310 | **+0.444** | **+0.933** | −0.427 |
| kinterp | +0.949 | +0.947 | **−0.146** | +0.435 | +0.924 | **−0.236** |

### 거절 규칙 적용 후 (coverage / 실제 R²)

| method | y0 | y1 | y2 | y3 | y4 | y5 |
|--------|-----|-----|-----|-----|-----|-----|
| bitint | 100% / +0.97 | 100% / +0.98 | **0%** | 22% / +0.47 | 100% / +0.93 | **0%** |
| kinterp | 100% / +0.95 | 100% / +0.95 | **0%** | 16% / +0.45 | 100% / +0.92 | **0%** |

---

## 3. 해석

**① 커널 보간이 "정확 통과"와 "좋은 보간"을 동시에 만족한다.**
nugget=0 커널 보간은 학습점을 오차 1e-13으로 통과하면서 y0/y1/y4에서 R² 0.92~0.95를 낸다.
룩업 테이블 없이도 1축 요구사항이 저절로 충족된다.

**② `expknn`과 `kinterp`는 같은 커널을 쓰는데 성능이 2배 차이난다 (+0.238 vs +0.477 평균).**
차이는 커널이 아니라 **푸는 방식**이다.
- `expknn`: 이웃 48개의 **가중평균**. 커널을 평활화 도구로 씀
- `kinterp`: `K·a = y`를 **풀어서** 커널끼리의 겹침을 역으로 걷어냄

이 연립방정식 풀이가 성능 차이 전부를 만든다. 고차원 이진 공간에서 KNN이 실패하는 건
"거리가 쓸모없어서"가 아니라 **"가중평균이 쓸모없어서"** 라는 뜻이다.

**③ y2(패리티)·y5(톱니)는 네 방식 모두 실패하고, 실패 양상이 다르다.**
- KNN: R² ≈ 0 — 모른다는 걸 알고 가만히 있음
- bitint: R² −0.31 / −0.43 — 모르면서 자신 있게 틀림
- kinterp: R² −0.15 / −0.24 — 틀리지만 **피해가 절반**

최적화기 입장에서는 세 번째가 가장 안전하다. 거절이 새더라도 손해가 작다.

**④ 거절 규칙이 의도대로 동작한다.** y2/y5가 두 좋은 모델 모두에서 coverage 0%로 전량 거절된다.

---

## 4. 예측이 빗나간 것 (기록)

리서치 단계에서 내린 예측 중 측정으로 뒤집힌 것들:

| 예측 | 실제 |
|---|---|
| "nugget=0 커널 보간은 평균+스파이크로 붕괴한다" | tau≤3에서만. tau=20에서는 R² +0.46 |
| "Tanimoto는 집중 현상으로 약할 것" | bitint와 동일 (+0.424 vs +0.423) |
| "최근접 이웃 해밍거리 ~25" | 실측 **19.5** |
| "거리 기반은 구조적으로 불가능" | 절반만 맞음. 가중평균이 문제지 거리가 문제가 아님 |

---

## 5. 이진 공간 보간 리서치

### Walsh(Fourier) 기저가 표준 프레임
모든 `f: {0,1}^n → R`는 Walsh 기저로 정확히 분해된다. "m개 샘플로 학습 가능한가"는 곧
"스펙트럼이 소수의/저차 항에 집중되어 있는가"와 같은 질문이다
([GECCO'19](https://dl.acm.org/doi/10.1145/3321707.3321800),
[PPSN'18](https://link.springer.com/chapter/10.1007/978-3-319-99259-4_15)).

`bitint`(2차 단항식)는 2차 Walsh와 같은 공간을 span한다. 차이는 조건수뿐 —
Walsh는 균등샘플링에서 직교라 ridge가 모든 항을 공평하게 축소한다.

### BOCS — `bitint`의 베이지안 버전
[Baptista & Poloczek, ICML 2018](https://arxiv.org/pdf/1806.08838). 2차 단항식 회귀에
horseshoe prior로 희소화. 알려진 한계가 **"저차 상호작용에 국한"** 인데,
y2/y5 실패가 버그가 아니라 모델 클래스의 구조적 한계임을 뒷받침한다.

### COMBO — 하이퍼큐브 그래프 푸리에
[Oh et al., NeurIPS 2019](http://papers.neurips.cc/paper/8557-combinatorial-bayesian-optimization-using-the-graph-cartesian-product.pdf).
이진 변수의 조합 그래프는 하이퍼큐브이고 그 그래프 푸리에 기저가 곧 Walsh 기저다.
확산 커널로 차수에 따라 매끄럽게 감쇠하는 가중치를 줘서, 고차 항을 열거하지 않고 담는다.

### Tanimoto/Jaccard 커널
`|교집합| / |합집합|`. PSD가 증명되어 있고 화학정보학 표준
([GAUCHE](https://arxiv.org/pdf/2212.04450), [Tanimoto random features](https://arxiv.org/html/2306.14809v1)).
**실측: 보간으로 쓰면 +0.424로 bitint와 동일.** 값의 범위는 0.035~0.702 (평균 0.334)로
예상보다 넓었다. 별도 구현의 이득이 없어 채택하지 않음.

### 희소 Walsh 복원 — 고차 항을 잡는 유일한 길
SPRIGHT / Kushilevitz–Mansour 계열은 K개 희소 Walsh 계수를 `O(K log n)` 샘플로 복원한다
([SPRIGHT](https://arxiv.org/pdf/1508.06336),
[Fourier sparse set functions](https://las.inf.ethz.ch/files/amrollahi19efficiently.pdf)).

**단, 구조화된 질의(designed query)를 요구한다. i.i.d. 랜덤 샘플로는 안 된다.**
y2는 10차 패리티 = Walsh 계수 딱 하나라 설계된 질의로는 정확 복원 대상이지만,
7000개 랜덤 샘플로는 불가능하다 — 10차 패리티는 모든 저차 통계량과 무상관이고
후보가 `C(70,10) ≈ 4×10¹¹`개다.

### 해밍거리 기반 trust region이 안 되는 이유
7000개 점 주변 반경 r의 공이 공간을 덮는 비율:

| 반경 r | 10 | 12 | 15 | 18 | 20 |
|---|---|---|---|---|---|
| 덮는 비율 | 0.0003% | 0.008% | 0.58% | 20% | 전체 |

의미 있으면서(r 작음) 비어 있지 않은(r 큼) 반경이 없다.
그래서 trust region은 거리가 아니라 **출력별 + popcount 범위**로 잡았다.

---

## 6. 다음 단계

1. **`kinterp` vs `bitint` 선택** — 아래 6-1 참고. 속도가 갈림길이다.
2. **tau 튜닝** — tau=12는 y2/y5 피해가 더 작고(−0.13/−0.14) y0/y1은 낮다(+0.93/+0.92).
   거절을 신뢰하면 tau=20~25, 안전을 택하면 tau=12.
3. **샘플링 분포** — uniform이라 popcount가 35±4에 몰려 있다(min 21, max 50).
   최적화기가 popcount 10이나 60을 탐색하면 전부 외삽이고 popcount 가드가 전량 거절한다.
4. **설계된 질의 도입 여부** — y2/y5를 포기할지, GOAL의 랜덤샘플링 전제를 풀지 결정 필요.

### 6-1. kinterp와 bitint의 실전 비용 차이

| | kinterp | bitint |
|---|---|---|
| 축1 (정확 통과) | 원래 통과 | 룩업 필요 |
| 축2 y0/y1/y4 | +0.95 / +0.95 / +0.92 | **+0.97 / +0.98 / +0.93** |
| 축2 y2/y5 피해 | **−0.15 / −0.24** | −0.31 / −0.43 |
| 질의 1건 비용 | 학습셋 전체와 거리 계산 ≈ 5×10⁵ 연산 | 특징 2486개 ≈ 2×10⁴ 연산 |
| 데이터 추가 시 재적합 | `O(n³)` | `O(n·p²)` |

**질의 비용이 약 30배 차이난다.** 최적화기가 대리모델을 수백만 번 호출하는 구조라면
이 차이가 지배적이다. 정확도는 kinterp가 미세하게 낮지만 위험은 더 낮다.

---

## 7. 실행

```bash
pip install numpy ruff pytest
python3 scorer.py     # 2축 채점 리포트 (약 11초)
ruff check . && ruff format --check .
python3 -m pytest     # 29 tests
```
