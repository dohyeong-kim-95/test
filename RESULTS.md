# 채점기 — 구현과 측정 결과

> 목표와 요구사항은 [GOAL.md](GOAL.md)가 원본이다. 이 문서는 그에 대한 구현과 측정 결과만 담는다.

## 1. 설계

### GT 함수 (`ground_truth`)
출력 6개가 각각 다른 난이도/구조를 갖도록 설계. 전부 `GT_SEED`로 고정되어 재현 가능.

| 출력 | 구조 | 미분불가능 요인 | Walsh 차수 |
|------|------|-----------------|-----------|
| y0 | 가중합의 abs 꺾임 3개 | `abs` | 낮음 |
| y1 | 랜덤 60쌍 pairwise AND → clip | `clip` | 2차 |
| y2 | 10비트 블록 7개의 **패리티** 가중합 | 패리티 | **10차** |
| y3 | 7-윈도우 popcount의 max·min | `max`/`min` | 중간 |
| y4 | 7비트 그룹 10개 점수의 max−min | `max`/`min` | 낮음~중간 |
| y5 | 가중합의 **톱니파** `\|mod(t,9)−4.5\|` | `mod`, `abs` | **매우 높음** |

### 예측기 (`Surrogate`, 클래스 1개)
- `idw` — 해밍거리 k=48 이웃, 가중치 `1/(d+0.5)`
- `expknn` — 같은 이웃, 가중치 `exp(-(d-d_min)/tau)` (언더플로 방지로 d_min 차감)
- `bitint` — `[1, bits(70), pairwise AND(2415)]` 특징에 ridge 회귀

### 신뢰도 / 거절 (`ensemble_predict` → `calibrate_threshold`)
세 방식 **공통**으로 같은 신뢰도 정의를 쓴다.

```
학습 데이터의 80%를 5번 다르게 뽑아 5개 모델을 적합
trust = −(5개 예측의 표준편차) / (출력 표준편차)      # 출력별로 따로
```

"데이터가 이 예측을 얼마나 단단히 고정하는가"를 직접 재는 값이라 방식과 무관하게 정의된다.

거절 임계값은 **테스트셋을 calib 1000 / eval 1000으로 나눠**, calib에서 `R² ≥ 0.5`를 만족하는
최대 coverage 지점의 trust 값을 임계값으로 잡고, eval에서만 성능을 보고한다.
(임계값을 평가셋에서 고르면 자기 자신을 속이게 된다.)

### 채점 로직 (`risk_coverage`)
단일 임계값 대신 **risk-coverage 곡선**. 신뢰도 상위 X%만 채점했을 때 오차가 실제로 줄어드는지를 본다 —
줄지 않으면 그 신뢰도 점수는 쓸모없다는 뜻.

- 지표: `nMAE = MAE / std`, `R² = 1 − MSE/var` (테스트셋 전체 분산 기준 고정 → 부분집합끼리 비교 가능)
- coverage 레벨: 100% / 80% / 50% / 20% / 5%, **출력별로** 따로 선택
- **베이스라인(train mean) 필수** — 안 그러면 "평균만 찍기"보다 나쁜지 알 수 없음

---

## 2. 실행 결과

```
train (7000, 70)  calib 1000  eval 1000
popcount train: mean 35.0 min 21 max 50
baseline (train mean)   nMAE 0.814   R2 +0.003
```

### 전체 coverage에서의 R²

| method | y0 | y1 | y2 | y3 | y4 | y5 |
|--------|-----|-----|-----|-----|-----|-----|
| idw    | +0.465 | +0.393 | +0.022 | +0.101 | +0.450 | −0.026 |
| expknn | +0.483 | +0.413 | +0.012 | +0.098 | +0.470 | −0.027 |
| bitint | **+0.971** | **+0.984** | −0.310 | **+0.444** | **+0.930** | −0.427 |

### 거절 규칙 적용 후 (`accept@R²≥0.5`, coverage / 실제 R²)

| method | y0 | y1 | y2 | y3 | y4 | y5 |
|--------|-----|-----|-----|-----|-----|-----|
| idw    | 78% / +0.56 | 0% | 0% | 2% / +0.28 | 1% / +0.38 | 0% |
| expknn | 83% / +0.58 | 0% | 0% | 1% / +0.40 | 26% / +0.50 | 0% |
| bitint | **100% / +0.97** | **100% / +0.98** | 0% | 22% / +0.47 | **100% / +0.93** | 0% |

### 해석

**① KNN 계열은 이 공간에서 구조적으로 작동하지 않는다.**
2^70 공간에 7000점이면 최근접 이웃 해밍거리가 ~25다. 거리 25짜리 이웃은 사실상 무작위 점이라
"국소 보간"이라는 전제 자체가 성립하지 않는다. R² 0.4~0.5는 이웃이 아니라 저차 성분이 새어나온 것.
튜닝으로 고칠 수 있는 문제가 아니다 (측도의 집중 현상).

**② bit intersection(2차 ridge)이 압도적.**
y0/y1/y4에서 R² 0.93~0.98. 7000점으로 2486개 계수를 정하는 건 충분히 가능하고,
70비트 공간에서는 국소 이웃보다 **전역 저차 구조**가 훨씬 값싸게 얻어진다.

**③ y2(패리티)·y5(톱니)는 세 방식 모두 실패, 그리고 실패 양상이 다르다.**
- KNN은 R² ≈ 0 (베이스라인 수준) — 모른다는 걸 알고 가만히 있음
- bitint는 R² = **−0.31 / −0.43** — 모르면서 자신 있게 틀림. 평균만 찍는 것보다 나쁨
- 이게 최적화기 입장에서 가장 위험하다. 틀린 방향으로 **적극적으로** 끌고 가기 때문

**④ 앙상블 불일치 기반 신뢰도는 작동한다.**
- KNN: y0 R²가 coverage 100%→20%에서 +0.465→+0.706으로 올라간다. 신뢰도가 실제로 좋은 점을 골라낸다
  (이전의 이웃-분산 기반 trust는 0.708→0.690으로 사실상 평평했다)
- bitint: y2/y5를 **coverage 0%로 전부 거절**한다. R²=−0.4짜리 예측이 밖으로 나가지 않는다
- 즉 `data lack` 거절이 의도대로 동작한다

---

## 3. 이진 공간 보간 리서치

70비트/7000점이라는 조건에서 무엇이 되고 무엇이 안 되는지에 대한 배경.

### Walsh(Fourier) 기저가 표준 프레임
모든 `f: {0,1}^n → R`는 Walsh 기저 `χ_S(x) = (−1)^(Σ_{i∈S} x_i)`로 **정확히** 분해된다 (계수 2^n개).
"m개 샘플로 학습 가능한가"는 곧 "스펙트럼이 소수의/저차 항에 집중되어 있는가"와 같은 질문이다.
pseudo-Boolean 최적화에서 Walsh 대리모델은 확립된 접근이다
([Walsh surrogate for pseudo-Boolean, GECCO'19](https://dl.acm.org/doi/10.1145/3321707.3321800),
[PPSN'18](https://link.springer.com/chapter/10.1007/978-3-319-99259-4_15)).

현재 `bitint`(2차 단항식)는 2차 Walsh와 **같은 공간**을 span한다. 차이는 조건수다:
Walsh는 균등샘플링 하에서 직교라 Gram이 단위행렬에 가깝고, ridge가 모든 항을 공평하게 축소한다.
단항식 기저는 `E[x_i x_j]=0.25` vs `E[x_i]=0.5`로 상관이 심해 ridge가 편향된다.
→ **같은 모델의 조건수 개선**으로 적용 가능.

### BOCS — 우리 `bitint`의 베이지안 버전
[Baptista & Poloczek, ICML 2018](https://arxiv.org/pdf/1806.08838)은 정확히 2차 단항식 회귀에
horseshoe prior를 걸어 희소화한다. 알려진 한계가 **"저차 상호작용에 국한된다"** 인데,
우리 y2/y5 실패가 버그가 아니라 이 모델 클래스의 구조적 한계임을 뒷받침한다.

### COMBO — 하이퍼큐브 그래프 푸리에
[Oh et al., NeurIPS 2019](http://papers.neurips.cc/paper/8557-combinatorial-bayesian-optimization-using-the-graph-cartesian-product.pdf).
이진 변수의 조합 그래프는 하이퍼큐브이고, 그 그래프 푸리에 기저가 곧 Walsh 기저다.
확산 커널로 **차수에 따라 매끄럽게 감쇠하는 가중치**를 주므로, 고차 항을 일일이 열거하지 않고도
암묵적으로 담을 수 있다. 차수 절단(degree truncation)의 대안.

### Tanimoto/Jaccard 커널 — "진짜" bit intersection model
`k(x,x') = |x∩x'| / (|x|+|x'|−|x∩x'|)`. PSD가 증명되어 있고 화학정보학의 이진 지문에서 표준
([GAUCHE](https://arxiv.org/pdf/2212.04450), [Tanimoto random features](https://arxiv.org/html/2306.14809v1)).
내가 구현한 것은 교집합을 *특징*으로 쓴 회귀이고, 이쪽은 교집합을 *커널*로 쓰는 kernel ridge/GP다 — **다른 모델**이다.

다만 우리 조건에서는 기대가 낮다: 균등 70비트에서 `|x∩x'| ≈ 17.5`, `|x∪x'| ≈ 52.5`라
거의 모든 쌍이 Tanimoto ≈ 0.33으로 몰린다. 해밍과 같은 집중 현상.
Tanimoto가 힘을 쓰는 건 지문이 **희소할 때**인데 우리는 밀도 0.5다.

### ARD Hamming 커널 — 평범한 KNN의 정공법 수리
`k(x,x') = exp(−Σ_i λ_i·[x_i ≠ x'_i])`. 비트마다 길이척도 `λ_i`를 학습하면
무관한 비트는 `λ≈0`이 되어 거리 계산에서 빠진다. 지금 KNN이 70비트를 전부 동등하게 세는 탓에
신호가 묻히는 문제를 직접 겨냥한다.

### 희소 Walsh 복원 — 고차 항을 잡는 유일한 길, 단 조건이 붙는다
SPRIGHT / Kushilevitz–Mansour / Goldreich–Levin 계열은 K개 희소 Walsh 계수를
`O(K log n)` 샘플로 복원한다 ([SPRIGHT](https://arxiv.org/pdf/1508.06336),
[Fourier sparse set functions](https://las.inf.ethz.ch/files/amrollahi19efficiently.pdf)).

**핵심 단서: 이 방법들은 구조화된 질의(designed queries)를 요구한다. i.i.d. 랜덤 샘플로는 안 된다.**
우리 y2는 10차 패리티 = Walsh 계수 딱 하나이므로 설계된 질의로는 정확 복원 대상이지만,
7000개 랜덤 샘플로는 사실상 불가능하다 — 10차 패리티는 모든 저차 통계량과 무상관이고
후보가 `C(70,10) ≈ 4×10^11`개다.

### 정리
| | 랜덤 7000점으로 가능? |
|---|---|
| 거리 기반 국소 보간 (KNN, Tanimoto) | ✗ 측도의 집중으로 구조적 불가 |
| 전역 저차 회귀 (bitint/Walsh/BOCS) | ✓ 유일하게 작동하는 축 |
| 고차 희소 항 (y2, y5) | ✗ 랜덤 샘플로는 불가, **설계된 질의**가 필요 |

즉 y2/y5를 잡으려면 모델을 바꾸는 게 아니라 **샘플링 설계를 바꿔야 한다.**
이건 GOAL의 "7000개 랜덤샘플링" 전제를 건드리는 변경이라 결정이 필요하다.

---

## 4. 다음 단계

1. **Walsh 기저로 교체** — `bitint`를 ±1 Walsh 2차 기저로. 직교성 덕에 조건수가 좋아지고,
   계수 임계처리로 희소화가 가능해진다. 모델 클래스는 그대로라 위험이 낮다.
2. **ARD Hamming 커널** — KNN 계열을 살릴 유일한 방향. 다만 위 한계상 기대치는 낮게 잡아야 한다.
3. **샘플링 분포** — 현재 uniform이라 popcount가 35±4에 몰려 있다(min 21, max 50).
   최적화기가 popcount 10이나 60 근처를 탐색하면 전부 외삽이다. popcount를 섞어서 뽑아야 함.
4. **설계된 질의 도입 여부** — y2/y5를 포기할지, GOAL의 랜덤샘플링 전제를 풀지 결정 필요.
5. **최적화기 연동 시 주의** — `bitint`의 y2/y5는 R²가 음수다. 거절 규칙을 반드시 통과시킬 것.

---

## 5. 실행

```bash
pip install numpy ruff pytest
python3 scorer.py     # 채점 리포트
ruff check . && ruff format --check .
python3 -m pytest     # 21 tests
```
