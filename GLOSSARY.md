# 용어집

이 저장소에서 쓰는 말들. 새 용어가 나오면 여기에 추가한다.

---

## 문제 설정

**대리모델 (surrogate)**
GT 계산식이 느리거나 비쌀 때 그걸 흉내 내는 싸고 빠른 근사 모델.
최적화기는 GT 대신 이걸 수백만 번 호출한다. 대리모델이 틀리면 최적화 전체가 틀린다.

**해밍거리 (Hamming distance)**
두 비트열에서 서로 다른 비트의 개수. `1010` vs `1001` → 2.

**popcount**
비트열에서 1의 개수. 70비트를 동전던지기로 뽑으면 평균 35.

**외삽 (extrapolation)**
학습 데이터가 없는 영역을 예측하는 것.
우리 데이터는 popcount 21~50에만 있어서, popcount 10짜리 질문은 외삽이다.

**i.i.d. (independent and identically distributed)**
매번 독립적으로 같은 방식으로 무작위로 뽑는 것. 우리 7000점이 이것.
반대는 **설계된 질의(designed query)** — 특정 패턴을 의도적으로 골라 물어보는 것.

---

## 왜 KNN이 안 되는가

**측도의 집중 (concentration of measure)**
차원이 높아지면 모든 점이 서로 고만고만하게 멀어지는 현상.

우리 숫자: 70비트 두 점의 해밍거리 기댓값 35, 표준편차 4.2.
7000개를 뽑아도 최근접 이웃이 ~25. 70개 중 25개가 다르면 사실상 남남이다.
KNN은 "가까운 점은 값도 비슷하다"를 전제하는데 가까운 점 자체가 없다.
데이터를 10배 늘려도 최근접거리는 1~2밖에 안 줄어든다 → 튜닝이 아니라 구조적 문제.

---

## 함수의 구조

**차수 (degree)**
몇 개의 비트가 **동시에** 얽혀야 값이 정해지는가.

| 차수 | 예 | 뜻 |
|---|---|---|
| 1차 | `3·x₅` | 비트 하나만 보면 됨 |
| 2차 | `3·x₅·x₉` | 두 비트를 같이 봐야 함 |
| 10차 패리티 | `x₀⊕x₁⊕…⊕x₉` | 10개를 전부 봐야 함. 9개를 알아도 답은 반반 |

`bitint`는 2차까지만 본다. 10차는 표현 자체가 불가능하다.

**Walsh 기저**
소리를 사인파의 합으로 분해하듯, 이진 함수를 **패리티 함수들의 합**으로 분해하는 것.
비트 부분집합 S마다 기저함수 `χ_S`가 하나씩 있고(S에 속한 비트들의 패리티를 ±1로 쓴 것),
70비트면 부분집합이 2⁷⁰개. 계수를 전부 알면 함수를 정확히 복원할 수 있다.

**스펙트럼 / 희소 (sparse)**
Walsh 계수들의 분포가 스펙트럼. 대부분 0이고 소수만 크면 "희소하다".
**학습 가능성 = 스펙트럼이 저차에 몰려 있는가.**
y0/y1/y4는 저차에 몰려 있어 잘 맞고, y2는 10차 계수 딱 하나라 2차 모델로는 하나도 못 잡는다.

**span (생성공간)**
어떤 기저로 만들 수 있는 함수 전체의 집합.
"2차 단항식과 2차 Walsh가 같은 공간을 span한다" = 표현 가능한 함수가 똑같다 = 성능 상한이 같다.
차이는 계산 안정성뿐.

---

## 회귀

**보간 (interpolation) vs 회귀 (regression)**
- **보간**: 학습 데이터 점을 **정확히** 통과해야 함. `f(xᵢ) = yᵢ`
- **회귀**: 통과할 필요 없음. 전체 오차만 작으면 됨

현재 `bitint`는 회귀다. 학습점에서도 조금씩 틀린다.

**ridge 회귀**
보통 회귀에 "계수를 너무 크게 만들지 마라"는 벌점을 더한 것.
특징 수가 많거나 서로 상관이 심할 때 필요. 우리는 특징 2486개 / 샘플 7000개라 필수.

**직교 (orthogonal) / 조건수 (condition number)**
직교 = 특징들이 서로 무관함.
우리 단항식 기저는 `xᵢ`와 `xᵢ·xⱼ`가 상관돼 있어(E[xᵢ]=0.5, E[xᵢxⱼ]=0.25) 서로 간섭한다.
조건수는 그 간섭의 심한 정도. 크면 계수 추정이 불안정.
Walsh는 균등샘플링에서 직교라 이 문제가 없다.

**커널 (kernel) / PSD (양반정부호)**
커널은 두 점의 유사도를 재는 함수. 이것만 정의하면 특징을 직접 안 만들고도 회귀할 수 있다.
PSD는 그 유사도 행렬이 수학적으로 올바른 형태라는 조건이고, 만족해야 안정적으로 풀린다.
"Tanimoto가 PSD"라는 건 바로 갖다 쓸 수 있다는 뜻.

**nugget (너깃)**
커널 회귀에서 대각선에 더하는 작은 값. ridge의 벌점과 같은 역할.
**nugget = 0이면 학습점을 정확히 통과(보간)하고, > 0이면 통과하지 않는다(회귀).**

**Tanimoto / Jaccard**
`|교집합| / |합집합|`. 둘 다 1인 비트 수 ÷ 하나라도 1인 비트 수.
우리 조건(밀도 0.5)에서는 교집합 ≈17.5, 합집합 ≈52.5라 거의 모든 쌍이 0.33으로 몰린다.
해밍과 똑같은 집중 현상.

---

## 채점

**R²**
`1 − (내 모델 오차) / (그냥 평균값 찍었을 때 오차)`
- `1.0` = 완벽
- `0.0` = 평균 찍는 것과 동일
- **음수 = 평균 찍는 것보다 나쁨** ← bitint의 y2(−0.31), y5(−0.43)

**nMAE**
`평균절대오차 / 표준편차`. 출력마다 스케일이 달라서 나눠준 것. 작을수록 좋다.

**risk-coverage**
- **coverage**: 거절하지 않고 답한 비율
- **risk**: 답한 것들의 오차

신뢰도 순으로 정렬해 상위 X%만 채점하고, X를 줄일 때 오차가 **줄어야** 그 신뢰도가 쓸모있다.
이전 방식은 100%→5%에서 0.708→0.690 (거의 안 줄어듦, 쓸모없음).
지금은 0.465→0.706 (확실히 줄어듦, 작동함).

**앙상블 불일치**
데이터 80%를 5번 다르게 뽑아 모델 5개를 만들고, 같은 질문에 5개가 얼마나 다르게 답하는지 재는 것.
많이 갈리면 = 데이터가 그 답을 못 정한 것 = 믿지 말라는 신호.

**calibration split**
임계값을 정하는 데이터와 성능을 보고하는 데이터를 분리하는 것.
같은 데이터로 둘 다 하면 자기 자신을 속이게 된다.

---

## 최적화

**trust region (신뢰 영역)**
원래는 최적화 용어로, "대리모델을 믿을 수 있는 범위". 최적화기를 그 안에서만 움직이게 하고,
예측이 잘 맞으면 영역을 넓히고 틀리면 좁힌다.

우리 맥락에서는 "예측을 받아들일 영역"이라는 뜻으로 쓴다.
**주의**: 70비트에서 해밍거리로 영역을 정의하면 잘 작동하지 않는다 (측도의 집중 때문).

**룩업 테이블 (lookup table)**
질문을 키로, 답을 값으로 저장해둔 표. 이미 아는 x가 들어오면 계산 없이 저장된 y를 그대로 돌려준다.

**메모이제이션 (memoization)**
계산 결과를 저장해뒀다가 같은 질문이 또 오면 재사용하는 것. 룩업 테이블을 쓰는 방식.
